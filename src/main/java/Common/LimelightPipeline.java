package Common;

/** Add your docs here. */
public class LimelightPipeline {
    public double id;
    boolean ledState;
  
    LimelightPipeline(double id, boolean ledState) {
      this.id = id;
      this.ledState = ledState;
    }
  }
